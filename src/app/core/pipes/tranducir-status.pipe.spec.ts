import { TranducirStatusPipe } from './tranducir-status.pipe';

describe('TranducirStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new TranducirStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
